import * as React from "react";
import styles from "./VendorConfig.module.scss";

const VendorConfig = (props: any): JSX.Element => {
  return <div>Vendor Config</div>;
};

export default VendorConfig;
